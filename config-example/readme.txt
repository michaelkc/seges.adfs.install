This folder contains a structurally correct config. 
The pfx files should obviously be real certificates, not 0-byte placeholders. 
All files should be placed next to Install-Adfs.ps1 / Join-Adfs.ps1
For actual values for SEGES DEV-DEVTEST-PRODTEST-PROD-SI, see Confluence